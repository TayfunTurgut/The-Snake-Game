var transX;
var transY;
var snake;

function setup() {
    createCanvas(windowWidth, windowHeight);
    transX = canvas.width * 0.5;
    transY = canvas.height * 0.5;
    background(51);
    snake = new Snake();
}

function draw() {
  
}
